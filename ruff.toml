[lint]
select = ["E", "F", "W", "I", "UP"]
ignore = [
  "E501",   # line too long — HA code often has long strings
]

[lint.per-file-ignores]
"custom_components/dribl/__init__.py" = ["F401"]

[format]
quote-style = "double"
indent-style = "space"
